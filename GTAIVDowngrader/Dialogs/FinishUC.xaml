<UserControl x:Class="GTAIVDowngrader.Dialogs.FinishUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GTAIVDowngrader.Dialogs"
             xmlns:Controls="clr-namespace:GTAIVDowngrader.Controls"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d"
             d:DesignHeight="509" d:DesignWidth="842">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Image Grid.RowSpan="2" Source="..\Resources\chars\char5.png" RenderOptions.BitmapScalingMode="HighQuality" Opacity="1" Stretch="Uniform" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,15,20,0"/>

        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="30,20,200,35">
            <TextBlock Text="Congratulations!" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
            <TextBlock TextWrapping="WrapWithOverflow" FontSize="14" Foreground="White" HorizontalAlignment="Left" Margin="10,0,0,0">
                Your GTA IV installation should be downgraded to your desired version!<LineBreak/>
                Make sure to always run GTA IV through Steam (If you own it on steam) or through <Run Text="PlayGTAIV.exe" FontWeight="Bold"/><LineBreak/><LineBreak/> 
                Have fun playing!
            </TextBlock>
        </StackPanel>

        <CheckBox x:Name="ShowLogCheckBox" Checked="ShowLogCheckBox_CheckedChanged" Unchecked="ShowLogCheckBox_CheckedChanged" Content="Show Log" FontSize="12" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="30,0,30,36"/>
        <ScrollViewer MaxHeight="104" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="30,270,30,55">
            <ListBox x:Name="LogItemsListBox" Visibility="Collapsed"/>
        </ScrollViewer>

        <TextBlock Text="Click on the 'Exit' button to exit the downgrader." FontSize="12" Foreground="#adadad" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="30,15"/>

        <Rectangle Grid.Row="0" Fill="#4Dffffff" Height="1" VerticalAlignment="Bottom"/>
        <Grid Grid.Row="1" Background="#B3000000">
            <Button x:Name="ExitButton" Click="ExitButton_Click" Content="Exit" IsEnabled="True" Background="#0050bf" Style="{StaticResource MetroButton}" Width="80" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="15,0"/>
            <Button x:Name="BackButton" Click="BackButton_Click" Visibility="Hidden" Content="Back" IsEnabled="True" Background="#0050bf" Style="{StaticResource MetroButton}" Width="80" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="105,0"/>
            <Button x:Name="PlayButton" Click="PlayButton_Click" Visibility="Visible" Content="Play GTA IV" IsEnabled="True" Background="#0050bf" Style="{StaticResource MetroButton}" Width="100" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="105,0"/>
        </Grid>
    </Grid>
</UserControl>
